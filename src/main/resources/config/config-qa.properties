# ============================================
# QA Environment Configuration
# ============================================

# API Base URL
api.base.url=https://reqres.in/api

# Note: report path/name/title are defined as Java constants
# in FrameworkConstants.java (not read from this file). Edit
# FrameworkConstants.java directly if you need to change them.

# Test Data
testdata.excel.path=src/test/resources/testdata/TestData.xlsx

# Credentials (override in CI via env vars if the API requires auth)
app.username=standard_user
app.password=secret_sauce
